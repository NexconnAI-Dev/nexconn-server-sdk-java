/*
 * Nexconn Server API
 * OpenAPI specification aligned with the current Nexconn public documentation, PDF source documents, and generated SDK requirements.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: customercare@nexconn.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package ai.nexconn;

import java.util.Map;
import java.util.List;

import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParser;

/**
 * API exception carrying HTTP status, headers, raw body, and parsed business error fields.
 */
@SuppressWarnings("serial")
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.14.0")
public class ApiException extends Exception {
    private static final long serialVersionUID = 1L;

    private int code = 0;
    private Map<String, List<String>> responseHeaders = null;
    private String responseBody = null;

    private int errorCode = -1;
    private String errorMessage = null;

    /**
     * <p>Constructor for ApiException.</p>
     */
    public ApiException() {}

    /**
     * <p>Constructor for ApiException.</p>
     *
     * @param throwable a {@link java.lang.Throwable} object
     */
    public ApiException(Throwable throwable) {
        super(throwable);
    }

    /**
     * <p>Constructor for ApiException.</p>
     *
     * @param message the error message
     */
    public ApiException(String message) {
        super(message);
    }

    /**
     * <p>Constructor for ApiException.</p>
     *
     * @param message the error message
     * @param throwable a {@link java.lang.Throwable} object
     * @param code HTTP status code
     * @param responseHeaders a {@link java.util.Map} of HTTP response headers
     * @param responseBody the response body
     */
    public ApiException(String message, Throwable throwable, int code, Map<String, List<String>> responseHeaders, String responseBody) {
        super(message, throwable);
        this.code = code;
        this.responseHeaders = responseHeaders;
        this.responseBody = responseBody;
        parseResponseBody();
    }

    /**
     * <p>Constructor for ApiException.</p>
     *
     * @param message the error message
     * @param code HTTP status code
     * @param responseHeaders a {@link java.util.Map} of HTTP response headers
     * @param responseBody the response body
     */
    public ApiException(String message, int code, Map<String, List<String>> responseHeaders, String responseBody) {
        this(message, (Throwable) null, code, responseHeaders, responseBody);
    }

    /**
     * <p>Constructor for ApiException.</p>
     *
     * @param message the error message
     * @param throwable a {@link java.lang.Throwable} object
     * @param code HTTP status code
     * @param responseHeaders a {@link java.util.Map} of HTTP response headers
     */
    public ApiException(String message, Throwable throwable, int code, Map<String, List<String>> responseHeaders) {
        this(message, throwable, code, responseHeaders, null);
    }

    /**
     * <p>Constructor for ApiException.</p>
     *
     * @param code HTTP status code
     * @param responseHeaders a {@link java.util.Map} of HTTP response headers
     * @param responseBody the response body
     */
    public ApiException(int code, Map<String, List<String>> responseHeaders, String responseBody) {
        this("Response Code: " + code + " Response Body: " + responseBody, (Throwable) null, code, responseHeaders, responseBody);
    }

    /**
     * <p>Constructor for ApiException.</p>
     *
     * @param code HTTP status code
     * @param message a {@link java.lang.String} object
     */
    public ApiException(int code, String message) {
        super(message);
        this.code = code;
    }

    /**
     * <p>Constructor for ApiException.</p>
     *
     * @param code HTTP status code
     * @param message the error message
     * @param responseHeaders a {@link java.util.Map} of HTTP response headers
     * @param responseBody the response body
     */
    public ApiException(int code, String message, Map<String, List<String>> responseHeaders, String responseBody) {
        this(code, message);
        this.responseHeaders = responseHeaders;
        this.responseBody = responseBody;
        parseResponseBody();
    }

    // ----------------------------------------------------------------
    // Factory: build typed subclass from an HTTP error response
    // ----------------------------------------------------------------

    /**
     * Create a typed {@link ApiException} subclass based on the HTTP status code.
     *
     * @param message       HTTP status message
     * @param httpStatus    HTTP status code
     * @param headers       response headers
     * @param body          raw response body (may be {@code null})
     * @return a concrete {@link ApiException} (never {@code null})
     */
    public static ApiException fromResponse(String message, int httpStatus, Map<String, List<String>> headers, String body) {
        switch (httpStatus) {
            case 400:
                return new BadRequestException(message, httpStatus, headers, body);
            case 401:
                return new UnauthorizedException(message, httpStatus, headers, body);
            case 403:
                return new ForbiddenException(message, httpStatus, headers, body);
            case 404:
                return new NotFoundException(message, httpStatus, headers, body);
            case 409:
                return new ConflictException(message, httpStatus, headers, body);
            case 422:
                return new UnprocessableEntityException(message, httpStatus, headers, body);
            case 429:
                return new TooManyRequestsException(message, httpStatus, headers, body);
            default:
                if (httpStatus >= 500) {
                    return new ServiceException(message, httpStatus, headers, body);
                }
                return new ApiException(message, httpStatus, headers, body);
        }
    }

    // ----------------------------------------------------------------
    // Business error fields parsed from response body JSON
    // ----------------------------------------------------------------

    private void parseResponseBody() {
        if (responseBody == null || responseBody.isEmpty()) {
            return;
        }
        try {
            JsonElement root = JsonParser.parseString(responseBody);
            if (!root.isJsonObject()) {
                return;
            }
            JsonObject obj = root.getAsJsonObject();
            if (obj.has("code")) {
                this.errorCode = obj.get("code").getAsInt();
            }
            if (obj.has("errorMessage")) {
                this.errorMessage = obj.get("errorMessage").getAsString();
            }
        } catch (Exception ignored) {
            // body is not valid JSON or missing expected fields; keep raw body only
        }
    }

    // ----------------------------------------------------------------
    // Getters
    // ----------------------------------------------------------------

    /**
     * Get the HTTP status code.
     *
     * @return HTTP status code
     */
    public int getCode() {
        return code;
    }

    /**
     * Get the HTTP response headers.
     *
     * @return A map of list of string
     */
    public Map<String, List<String>> getResponseHeaders() {
        return responseHeaders;
    }

    /**
     * Get the HTTP response body.
     *
     * @return Response body in the form of string
     */
    public String getResponseBody() {
        return responseBody;
    }

    /**
     * Get the business error code from the response body JSON {@code "code"} field.
     * Returns {@code -1} if the response body could not be parsed.
     *
     * @return business error code
     */
    public int getErrorCode() {
        return errorCode;
    }

    /**
     * Get the business error message from the response body JSON {@code "errorMessage"} field.
     * Returns {@code null} if the response body could not be parsed.
     *
     * @return business error message, or {@code null}
     */
    public String getErrorMessage() {
        return errorMessage;
    }

    /**
     * Get the exception message including HTTP response data.
     *
     * @return The exception message
     */
    public String getMessage() {
        return String.format("Message: %s%nHTTP response code: %s%nHTTP response body: %s%nHTTP response headers: %s",
                super.getMessage(), this.getCode(), this.getResponseBody(), this.getResponseHeaders());
    }

    // ================================================================
    // Typed subclasses
    // ================================================================

    /** HTTP 400 Bad Request. */
    public static class BadRequestException extends ApiException {
        public BadRequestException(String message, int code, Map<String, List<String>> headers, String body) {
            super(message, code, headers, body);
        }
    }

    /** HTTP 401 Unauthorized. */
    public static class UnauthorizedException extends ApiException {
        public UnauthorizedException(String message, int code, Map<String, List<String>> headers, String body) {
            super(message, code, headers, body);
        }
    }

    /** HTTP 403 Forbidden. */
    public static class ForbiddenException extends ApiException {
        public ForbiddenException(String message, int code, Map<String, List<String>> headers, String body) {
            super(message, code, headers, body);
        }
    }

    /** HTTP 404 Not Found. */
    public static class NotFoundException extends ApiException {
        public NotFoundException(String message, int code, Map<String, List<String>> headers, String body) {
            super(message, code, headers, body);
        }
    }

    /** HTTP 409 Conflict; e.g. resource already exists. */
    public static class ConflictException extends ApiException {
        public ConflictException(String message, int code, Map<String, List<String>> headers, String body) {
            super(message, code, headers, body);
        }
    }

    /** HTTP 422 Unprocessable Entity. */
    public static class UnprocessableEntityException extends ApiException {
        public UnprocessableEntityException(String message, int code, Map<String, List<String>> headers, String body) {
            super(message, code, headers, body);
        }
    }

    /** HTTP 429 Too Many Requests; rate limited. */
    public static class TooManyRequestsException extends ApiException {
        public TooManyRequestsException(String message, int code, Map<String, List<String>> headers, String body) {
            super(message, code, headers, body);
        }
    }

    /** HTTP 5xx Server Error. */
    public static class ServiceException extends ApiException {
        public ServiceException(String message, int code, Map<String, List<String>> headers, String body) {
            super(message, code, headers, body);
        }
    }
}
